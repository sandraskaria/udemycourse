<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.databinding">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DataBinding"
        tools:targetApi="31">

        <activity
            android:name="com.udemysample.navigation.NavigationSampleActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.DataBinding.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.example.viewmodel.ViewModelActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name="com.example.livedata.LiveDataActivity" />
        <activity android:name="com.example.livedata.student.StudentActivity" />
        <activity android:name="com.example.viewmodel.ViewModelDataBindActivity" />
        <activity android:name="com.example.viewmodel.LiveDataWithDataBindActivity" />
        <activity android:name="com.example.twowaydatabind.TwoWayBindActivity" />
        <activity android:name="com.example.twowaydatabind.sample.TwoWayAddActivity" />
    </application>

</manifest>